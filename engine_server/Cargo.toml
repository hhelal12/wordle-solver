[package]
name = "engine_server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8", features = ["macros", "json"] }
shared = { path = "../shared" }
tokio = { version = "1.53.0", features = ["full"] }
